import { font, colors } from './variables';

export const styles = {
  error: {
    color: colors.error,
    fontSize: font.fontSize2,
  },
  bold: {
    fontWeight: 'bold',
  },
};

export default styles;
